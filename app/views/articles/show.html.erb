<h1><%= @article.title %>
<%= link_to '<i class="fas fa-pen"></i>'.html_safe, edit_article_path(@article) %>
<%= link_to '<i class="fas fa-trash-alt"></i>'.html_safe, article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>
</h1>
<p><%= @article.content %></p>
<%= link_to 'Go back to homepage', articles_path %>